
On /vue_website folder you will find the Vue.js website that accesses the NodeJS API on localhost.
On /basic_website folder there is a vanilla JS website.
All NodeJS files are on root, /modules, and /routes folders.
